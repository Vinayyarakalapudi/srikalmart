# MongoDB Configuration Options

# Option 1: Use encoded password (recommended for security)
# To encode your password: node -e "console.log(Buffer.from('your_password').toString('base64'))"
DB_PASSWORD_ENCODED=WWluYXlAMTIz  

# Option 2: Use plain password (less secure)
# DB_PASSWORD=your_actual_password

# Option 3: Use full connection string
# MONGODB_URI=mongodb+srv://yvinay7989:your_password@srikalmart.pmu6mqa.mongodb.net/mern-ecommerce?retryWrites=true&w=majority

# Database configuration components (used when building from parts)
DB_USERNAME=yvinay7989
DB_CLUSTER=srikalmart.pmu6mqa.mongodb.net
DB_NAME=mern-ecommerce

# Local MongoDB (fallback - no configuration needed)
# mongodb://localhost:27017/mern-ecommerce

# Other environment variables
PORT=5000
NODE_ENV=development
JWT_SECRET=your_jwt_secret_here
